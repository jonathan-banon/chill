{% extends 'base.html.twig' %}
{% block body %}
    <div class="d-flex justify-content-around logo-container">
        <div class="d-flex flex-column justify-content-center align-items-center">
            <h2>Film</h2>
            <div class="logo d-flex align-items-center">
                <a href="{{ path('film_index') }}">
                    <img src="{{ asset('build/film.png') }}" class="img-fluid" alt="logo">
                </a>
            </div>
        </div>
        <div class="d-flex flex-column justify-content-center align-items-center">
            <h2>Jeux-vidéo</h2>
            <div class="logo d-flex align-items-center">
                <a href="{{ path('game_index') }}">
                    <img src="{{ asset('build/jeux-video.png') }}" class="img-fluid" alt="logo">
                </a>
            </div>
        </div>
        <div class="d-flex flex-column justify-content-center align-items-center">
            <h2>Série</h2>
            <div class="logo d-flex align-items-center">
                <a href="{{ path('serie_index') }}">
                    <img src="{{ asset('build/serie.svg') }}" class="img-fluid" alt="logo">
                </a>
            </div>
        </div>
    </div>
{% endblock %}
